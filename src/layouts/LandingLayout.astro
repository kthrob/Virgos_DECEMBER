---
import Announcement from '~/components/widgets/Announcement.astro';
import Footer from '~/components/widgets/Footer.astro';
import NavGlobal from '~/components/widgets/NavGlobal.astro';
import Layout from '~/layouts/BaseLayout.astro';
import NavLocal from '~/components/widgets/NavLocal.astro';
import SkipToContent from '~/components/widgets/SkipToContent.astro';

import type { NavLink, IconLink, MetaData } from '~/types';
export interface Props {
	meta: MetaData;
	globalLinks: Array<NavLink>;
	localLinks: Array<NavLink>;
	socialLinks: Array<IconLink>;
}
const { meta, localLinks, globalLinks, socialLinks } = Astro.props;
---

<Layout {meta}>
	<SkipToContent />
	<Announcement />
	<NavGlobal globalLinks={globalLinks} socialLinks={socialLinks} />
	<slot name="splash" />
	<main class="relative">
		<NavLocal localLinks={localLinks} />
		<slot />
	</main>
	<Footer />
</Layout>
